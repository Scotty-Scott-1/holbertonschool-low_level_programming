#include "main.h"
/**
* _isdigit - a function
* @c: a int
*
* Description - a function
*
* Return: int
*/
int _isdigit(int c)
{

	if (c >= 'a' && c <= 'z')
	{
	return (0);
	}
	else if (c >= 'A' && c <= 'Z')
	{
	return (0);
	}
	
	{
	return (1);
	}
}
