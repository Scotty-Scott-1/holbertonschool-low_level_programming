#include "main.h"
/**
* _isdigit - a function
* @c: a int
*
* Description - a function
*
* Return: int
*/
int _isdigit(int c)
{

	if (c >= '0' && c <= '0')
	{
	return (1);
	}
	else
	{
	return (0);
	}
}
