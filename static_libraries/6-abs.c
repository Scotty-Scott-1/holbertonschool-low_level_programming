#include "main.h"
/**
* _abs - Print the absolute value of an integer
* @a: a variable
* Descrition - a function
*
* Return: int
*/
int _abs(int a)
{

	if (a < 0)
	{
	a = (a * -1);
	return (a);
	}
	else
	{
	return (a);
	}
}
